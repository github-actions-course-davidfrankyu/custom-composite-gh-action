name: 'Get & Cache Dependencies'
description: 'Get the dependencies (via npm) and cache them.'
runs:
  using: 'composite'
  steps:
    - name: ECHO STUFF
      run: |
        echo "HELLO WORLD"
        echo "_ACTIONS LS v3"
        ls -a /__w/_actions/actions/checkout/v3
        echo "_ACTIONS LS v3.completed"
        ls -a /__w/_actions/actions/checkout/v3.completed
        echo "GH-CONTAINERS LS"
        ls -a /__w/gh-containers/gh-containers
        # ls -a ${{ github.action_path }}
        echo "${{ github.action_path }}"
        pwd
        ls -a
      shell: bash
